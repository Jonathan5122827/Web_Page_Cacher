compiling: run the command "make" to generate the  wxclient exe.
Run the command make "make clean" to delete the exe.

The wcclient must be run on cse04, then type "./wcclient"

bug: If the program segemnetation faults, or the program outpust error messegage, it because the url entered is not good. Restart the program and do not use that url again. 
